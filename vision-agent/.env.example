# Stream — reuse from the parent project
STREAM_API_KEY=
STREAM_API_SECRET=

# OpenAI
OPENAI_API_KEY=

# Target language taught (default: Spanish).
# Override per-deployment or encode in call_id as "<Language>:<uuid>".
TEACH_LANGUAGE=Spanish
