EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=

# Stream
STREAM_API_KEY=
STREAM_API_SECRET=
EXPO_PUBLIC_STREAM_API_KEY=

# Vision Agent server URL (server-side only — never use EXPO_PUBLIC_ prefix)
# Start the agent with: cd vision-agent && python -m uv run python main.py serve
# Default when running locally:
VISION_AGENT_URL=http://localhost:8000
